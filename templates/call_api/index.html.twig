{% extends 'base.html.twig' %}

{% block body %}
    <h1>Products</h1>
    <ul>
    {% for product in products %}
        <li class="product">{{ product.id }}, {{ product.name }} - {{ product.price }}</li>
    {% endfor %}
    </ul>
{% endblock %}
